/**
 * User roles Enumeration
 * @enum {string} user_roles
 * @property {string} ADMIN - administrator
 * @property {string} SELLER - seller
 * @property {string} USER - regular user
 */
export enum UserRoles {
  ADMIN = 'admin',
  SELLER = 'seller',
  USER = 'user',
}
